name: test-environment
channels:
  - defaults
dependencies:
  - python=3.5
  # required
  #- geopy
  - matplotlib
  - pillow
  - rasterio
  - requests
  # testing
  - pip
  - pytest
  - pytest-cov
  - pip:
    - geopy
    - mercantile

